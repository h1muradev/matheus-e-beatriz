const openBtn=document.getElementById('openExperience');const experience=document.getElementById('experience');const replay=document.getElementById('replay');const hearts=document.querySelector('.hearts');function reveal(){experience.classList.add('open');setTimeout(()=>experience.scrollIntoView({behavior:'smooth',block:'start'}),120)}openBtn?.addEventListener('click',reveal);replay?.addEventListener('click',()=>window.scrollTo({top:0,behavior:'smooth'}));function spawnHeart(){const el=document.createElement('span');el.className='heart-float';el.textContent=Math.random()>.45?'♡':'♥';el.style.left=Math.random()*100+'vw';el.style.fontSize=12+Math.random()*18+'px';el.style.animationDuration=7+Math.random()*8+'s';hearts.appendChild(el);setTimeout(()=>el.remove(),16000)}setInterval(spawnHeart,650);for(let i=0;i<7;i++)setTimeout(spawnHeart,i*280);