const openBtn=document.getElementById('openExperience');const experience=document.getElementById('experience');const replay=document.getElementById('replay');const hearts=document.querySelector('.hearts');const daysTogether=document.getElementById('daysTogether');function reveal(){experience.classList.add('open');setTimeout(()=>experience.scrollIntoView({behavior:'smooth',block:'start'}),120);for(let i=0;i<12;i++)setTimeout(spawnHeart,i*90)}openBtn?.addEventListener('click',reveal);replay?.addEventListener('click',()=>window.scrollTo({top:0,behavior:'smooth'}));function spawnHeart(){const el=document.createElement('span');el.className='heart-float';el.textContent=Math.random()>.45?'♡':'♥';el.style.left=Math.random()*100+'vw';el.style.fontSize=12+Math.random()*18+'px';el.style.animationDuration=7+Math.random()*8+'s';hearts.appendChild(el);setTimeout(()=>el.remove(),16000)}setInterval(spawnHeart,650);for(let i=0;i<7;i++)setTimeout(spawnHeart,i*280);function updateDays(){if(!daysTogether)return;const start=new Date('2026-08-07T00:00:00-03:00');const now=new Date();const diff=Math.max(0,Math.floor((now-start)/(1000*60*60*24)));daysTogether.textContent=String(diff)}updateDays();